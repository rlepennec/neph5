<div class="chat">

    <div class="chat-header">

{{log .}}

        <img class="chat-image" src="{{actor.token.img}}"/>

        <span class="chat-title">{{actor.name}} {{action.sentence}}</span>

        {{#if action.target}}
        <img class="chat-image" src="{{action.target.img}}"/>
        {{/if}}

    </div>

    <div class="chat-body">

        <div class="chat-details">

            {{#if (gte action.difficulty 0)}}
            <span>Difficulté: {{action.difficulty}}0%</span>
            {{/if}}

        </div>

    </div>

</div>