{{#if title}}
<h1>{{translate title}}</h1>
{{/if}}

<p
    data-action="open"
    data-uuid="{{references.reference.uuid}}"
    data-id="{{references.documentName}}.{{references.type}}.{{references.reference.id}}">{{references.reference.name}}</p>
