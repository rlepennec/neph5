{{#if title}}
<h1>{{translate title}}</h1>
{{/if}}

<select name={{name}}>
    {{#select value}}
    {{#each options as |option|}}
    <option value="{{option}}" class="option">{{option}}</option>
    {{/each}}
    {{/select}}
</select>



