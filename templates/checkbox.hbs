{{#if title}}
<h1>{{translate title}}</h1>
{{/if}}

<div class="checkbox{{#if document.sheet.locked}} locked{{/if}}">

    <input

        {{#if (or (nonNull difficulty) (lookup document.sheet "locked"))}}
            disabled
        {{/if}}

        {{#if value}}
            checked
        {{/if}}

        {{#if class}}
            class="{{class}}"
        {{/if}}

        id="{{name}}"
        name="{{name}}"
        type="checkbox"
        data-dtype="Boolean"/>

    <label for="{{name}}"/>

</div>