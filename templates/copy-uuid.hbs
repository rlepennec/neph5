{{#if debug}}
<button type="button" class="copy-uuid" data-action="copy" data-tooltip="{{translate 'copyId'}}" aria-label="{{translate 'copyId'}}">
    <i class="fa-sharp fa-solid fa-book-copy" inert=""></i>
    <span>{{translate 'copyId'}}</span>
</button>
{{/if}}