<section class="item-root"s>

    <aside style="--background-side: var(--background-side-competence)"></aside>

    <article>

        {{> "systems/neph5e/templates/item-header.hbs"}}

        <main>

            {{> "systems/neph5e/templates/item-select.hbs"
                title="element"
                name="system.base.element"
                value=document.system.base.element
                options=context.elements
            }}

            <h1>{{translate "description"}}</h1>

            {{#if document.sheet.locked}}
            {{{enrichedDescription}}}
            {{else}}
            <prose-mirror
                name="system.base.description"
                button="true"
                editable="{{editable}}"
                toggled="false"
                value="{{document.system.base.description}}">
                {{{enrichedDescription}}}
            </prose-mirror>
            {{/if}}

        </main>

    </article>

</section>
