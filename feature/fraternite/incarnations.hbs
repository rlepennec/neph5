<main 
    class='tab incarnations'
    data-group='primary'
    data-tab='incarnations'
    data-drop="true">

    {{#if (eq from "figure")}}
    <div class="incarnations-header">

        <div class="points tooltip">
            <img class="circle"/>
            <input
                {{#if actor.locked}}disabled{{/if}}
                class="middle"
                name="system.pointsIncarnations"
                placeholder="0"
                type="text"
                value="{{system.pointsIncarnations}}"
                data-dtype="Number"/>
            <span class="tooltiptext">{{translate "pointsIncarnations"}}</span>
        </div>

        <div class="points tooltip">
            <img class="circle"/>
            <input
                {{#if actor.locked}}disabled{{/if}}
                name="system.sapienceDepensee"
                placeholder="0"
                type="text"
                value="{{system.sapienceDepensee}}"
                data-dtype="Number"/>
            <span class="tooltiptext">{{translate "sapienceDepensee"}}</span>
        </div>

        <div class="points tooltip">
            <img class="circle"/>
            <input
                {{#if actor.locked}}disabled{{/if}}
                name="system.sapience"
                placeholder="0"
                type="text"
                value="{{system.sapience}}"
                data-dtype="Number"/>
            <span class="tooltiptext">{{translate "sapienceCourante"}}</span>
        </div>
        
    </div>
    {{/if}}

    {{#if (isEmptyCollection actor.periodes)}}
    <div class="helpPeriode">
        {{#if (eq from "figure")}}
        <label>{{translate "aideIncarnations"}}</label>
        {{else}}
        <label>{{translate "aideHistorique"}}</label>
        {{/if}}
        <i class="fas fa-arrow-alt-circle-down fa-2xl"></i>
    </div>
    {{/if}}

    {{#each actor.periodes as |periode|}}
    {{#unless (and (not ../isGM) (not periode.embedded.actif))}}
    <ol class="periodes item-list">

        <li class="periode draggable item" draggable="true" data-drag="true" data-fsid="{{periode.embedded.fsid}}" data-sid="{{periode.original.sid}}" data-item-id="{{periode.embedded.id}}">

            <div class="periode-header">

                <img class="puce"/>

                <img class="separator"/>

                {{#if (eq ../editedPeriode periode.original.sid)}}                
                <label data-action="deletePeriode" class="fas fa-trash delete"></label>
                {{/if}}

                <label data-action="displayPeriode" class="periode-name display {{#if (eq ../actor.system.periode periode.original.sid)}}current{{/if}} {{#unless periode.embedded.actif}}disabled{{/unless}}">{{periode.original.name}}: {{periode.original.contexte}}, {{periode.original.aube}}</label>

                <label class="periode-zone">...</label>

                <ul class="periode-menu">

                    <li data-action="editPeriode" class="edit">{{#if (eq ../editedPeriode periode/original.sid)}}Afficher{{else}}Editer{{/if}}</li>

                    {{#if ../isGM}}<li data-action="activatePeriode" class="activate">{{#if periode.embedded.actif}}Désactiver{{else}}Activer{{/if}}</li>{{/if}}

                    {{#unless (eq ../actor.system.periode periode.original.sid)}}
                    <li data-action="currentPeriode" class="define">Période courante</li>
                    {{/unless}}

                </ul>

            </div>

            <div class="periode-body"
                 {{#unless (or (includes ../elapsedPeriodes periode.original.sid) (eq ../editedPeriode periode.original.sid))}}
                 style="display: none;"
                 {{/unless}}>

                {{#unless (isEmptyCollection periode.embedded.actors)}}
                <ol class="members">

                    {{#each periode.embedded.actors as |actor|}}
                    <li class="member actor" data-id="{{actor.id}}">
                    <div class="definition">

                        <i class="fa-solid {{#if actor.newer}}fa-user-plus{{else}}fa-user-minus{{/if}}"></i>

                        <label data-action="openItem" class="name open">{{actor.name}}</label>

                        {{#if (eq ../../editedPeriode ../original.sid)}}
                        <label class="fas fa-trash delete"></label>
                        {{/if}}

                    </div>
                    </li>
                    {{/each}}

                </ol>
                {{/unless}}

                {{#unless (isEmptyCollection periode.embedded.vecus)}}
                <ol class="vecus">

                    {{#each periode.embedded.vecus as |item|}}
                    <li class="vecu item" data-id="{{item.id}}" data-sid="{{item.sid}}">
                    <div class="definition">

                        <input
                            {{#unless (eq ../../editedPeriode ../original.sid)}}disabled{{/unless}}
                            class="degre set"
                            type="number"
                            value="{{item.degre}}"
                            data-dtype="Number"/>

                        <label data-action="openItem" class="name open">{{item.name}}</label>

                        {{#if (eq ../../editedPeriode ../original.sid)}}                
                        <label data-action="deleteEmbedded" class="fas fa-trash delete"></label>
                        {{/if}}

                    </div>
                    </li>
                    {{/each}}

                </ol>
                {{/unless}}

                {{#unless (isEmptyCollection periode.embedded.focus)}}
                <ol class="periode-focus">

                    {{#each periode.embedded.focus as |item|}}
                    <li class="focus item" data-id="{{item.id}}" data-sid="{{item.sid}}">
                    <div class="focus-definition">

                        <a><i class="fas fa-scroll"></i></a>

                        <label data-action="openItem" class="focus-name open">{{item.name}}</label>

                        {{#if (eq ../../editedPeriode ../original.sid)}}
                        <label data-action="deleteEmbedded" class="fas fa-trash delete"></label>
                        {{/if}}

                    </div>
                    </li>
                    {{/each}}

                </ol>
                {{/unless}}

                {{#unless (isEmptyCollection periode.embedded.capacites)}}
                
                <ol class="periode-focus">

                    {{#each periode.embedded.capacites as |item|}}
                    <li class="focus item" data-id="{{item.id}}" data-sid="{{item.sid}}">
                    <div class="focus-definition">

                        <a><i class="fa-solid fa-pen-fancy"></i></a>

                        <label data-action="openItem" class="focus-name open">{{item.name}}</label>

                        {{#if (eq ../../editedPeriode ../original.sid)}}
                        <label data-action="deleteEmbedded" class="fas fa-trash delete"></label>
                        {{/if}}

                    </div>
                    </li>
                    {{/each}}

                </ol>
                {{/unless}}

            </div>

        </li>

    </ol>
    {{/unless}}
    {{/each}}

</div>