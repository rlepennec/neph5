<section class="item-root"s>

    <aside style="--background-side: var(--background-side-periode)"></aside>

    <article>

        {{> "systems/neph5e/templates/item-header.hbs"}}

        <main>

            {{> "systems/neph5e/templates/item-input.hbs"
                title="epoque"
                name="system.item.base.epoque"
                value=document.system.item.base.epoque
            }}

            {{> "systems/neph5e/templates/item-input.hbs"
                title="region"
                name="system.item.base.region"
                value=document.system.item.base.region
            }}

            {{> "systems/neph5e/templates/item-references.hbs"
                title="vecus"
                references=context.vecus
            }}

            <h1>{{translate "description"}}</h1>

            {{#if document.sheet.locked}}
            {{{enrichedDescription}}}
            {{else}}
            <prose-mirror
                name="system.item.base.description"
                button="true"
                editable="{{editable}}"
                toggled="false"
                value="{{document.system.item.base.description}}">
                {{{enrichedDescription}}}
            </prose-mirror>
            {{/if}}

        </main>

    </article>

</section>
