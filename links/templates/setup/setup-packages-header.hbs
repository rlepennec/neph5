<nav class="tabs">
    {{#each tabs as |tab|}}
    <h2 class="divider {{tab.cssClass}}" data-action="tab" data-group="{{tab.group}}" data-tab="{{tab.id}}">
        <i class="{{tab.icon}}" inert></i>
        <span class="tab-title">{{localize tab.label}}</span>
    </h2>
    {{/each}}
</nav>
